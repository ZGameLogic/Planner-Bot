import React from 'react';
import { PlanAction } from '../../types/APITypes.ts';

type EventActionsProps = {
  actions: [PlanAction]
}

function EventActions({ actions }: EventActionsProps): React.JSX.Element {
  return <></>;
}

export default EventActions;
